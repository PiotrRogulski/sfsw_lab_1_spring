import 'package:flutter/material.dart';

class ForcesGraph extends StatelessWidget {
  const ForcesGraph({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.red,
    );
  }
}
