import 'package:flutter/material.dart';

class PositionGraph extends StatelessWidget {
  const PositionGraph({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.blue,
    );
  }
}
