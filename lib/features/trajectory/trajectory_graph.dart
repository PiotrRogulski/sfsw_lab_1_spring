import 'package:flutter/material.dart';

class TrajectoryGraph extends StatelessWidget {
  const TrajectoryGraph({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.orange,
    );
  }
}
