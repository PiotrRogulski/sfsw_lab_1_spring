import 'package:flutter/material.dart';

class SpringVisualization extends StatelessWidget {
  const SpringVisualization({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.purple,
    );
  }
}
